{% set name = "casa_formats_io" %}
{% set version = "0.2.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 2ef410c8b66bce65469da0d62fa55fdefad4cccdb5df5396504e0f3c101cb7bc

build:
  skip: true  # [py<38]
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - setuptools
    - setuptools-scm
    - wheel
    - numpy
    - pip
  run:
    - python
    - astropy >=4.0
    - dask-core >=2.0
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - casa_formats_io
  commands:
    - pip check
  requires:
    - pip

about:
  home: http://casa-formats-io.readthedocs.org
  summary: Dask-based reader for CASA data
  license: LGPL-2.0-only
  license_file: LICENSE

extra:
  recipe-maintainers:
    - keflavich
    - mwcraig
    - astrofrog
